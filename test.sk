on load:
	DbConnect("mongodb+srv://admin:Jacob090@cluster0.xifqi.mongodb.net/myFirstDatabase?retryWrites=true&w=majority", "Global", "players")

command /getdata:
	trigger:
		set {_uuid} to GetPlayerData(player, "Uuid", "performance")
		send "&fYour Uuid: &7%{_uuid}%"
